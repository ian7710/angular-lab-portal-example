import { SsnMaskDirective } from './ssn-mask.directive';

describe('SsnMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnMaskDirective();
    expect(directive).toBeTruthy();
  });
});
