import { SsnMaskTwoDirective } from './ssn-mask-two.directive';

describe('SsnMaskTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnMaskTwoDirective();
    expect(directive).toBeTruthy();
  });
});
